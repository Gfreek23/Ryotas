<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TrialProject3" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/black</item>
<!--        <item name="colorPrimaryDark">@color/white</item>-->
    </style>

    <style name="Theme.TrialProject3" parent="Base.Theme.TrialProject3" />
    <style name="textStyle" parnet="Widget.AppCompat.TextView">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="textStyleBold" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Widget.AppCompat.SearchView.ActionBar" parent="Base.Widget.AppCompat.SearchView.ActionBar">
        <!-- Customize the background color -->
        <item name="android:background">@color/white</item>
        <!-- Customize the text color -->
        <item name="android:editTextColor">@color/black</item>
        <!-- Customize the hint text color -->
        <item name="android:textColorHint">@color/black</item>
    </style>

</resources>